document.querySelector("form").addEventListener("button", function platoAddRecipes(recipe) {
    let receta = document.getElementById("recipes").value;
    sessionStorage.setItem("recipes", id);

  })